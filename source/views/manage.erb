<div class="manage-body">
    <div class="manage-db-content">
    
        <% if @db_content.empty? %>

            <p>Databasen är tom.</p>

        <% else %>

            <% @db_content.each do |f| %>

                <div class="manage-item">
                    <div class="manage-db-id">
                        <p><%= f["id"] %></p>
                    </div>
                    <div class="manage-db-picture">
                        <img class="manage-db-picture" src="<%= f["filepath"] %>">
                    </div>
                    <div class="manage-db-name">
                        <p><%= f["name"] %></p>
                    </div>
                </div>
            <% end %>
            
        <% end %>

<!--
    <% if @db_content.any? %>
        <% @db_content.each do | f | %>

            <div class="manage-item">
                <div class="manage-db-id">
                    <p><%=f["id"]%></p>
                </div>
                <div class="manage-db-picture">
                    <img class="manage-db-picture" src="<%=f["filepath"]%>">
                </div>
                <div class="manage-db-name">
                    <p><%=f["name"]%></p>
                </div>
            </div>

        <% end %>
    <% else %>
        <p>Ingen data tillgänglig i databasen.</p>
    <% end %>


    <% @db_content.each do | f | %>

        <div class="manage-item">
            <div class="manage-db-id">
                <p><%=f["id"]%></p>
            </div>
            <div class="manage-db-picture">
                <img class="manage-db-picture" src="<%=f["filepath"]%>">
            </div>
            <div class="manage-db-name">
                <p><%=f["name"]%></p>
            </div>
        </div>
        -->

    <% end %>

    </div>
    <div class="manage-interface">

        <div class="manage-interface-container">

            <div>
                <form class="manage-submit-form" method="post">
                    <input class="manage-submit-file-upload-button" type="file" name="" id="">
                    <input class="manage-submit-name-field" type="text" name="" id="" placeholder="Namn: ">
                    <input class="manage-submit-person-button" type="submit" value="Lägg till">
                </form>
            </div>

            <div class="manage-spacer"></div>

            <div>
                <form class="manage-delete-form" method="POST">
                    <input class="manage-delete-number-field" type="number">
                    <div class="manage-delete-form-spacer"></div>
                    <input class="manage-delete-person-button" type="submit" value="Ta bort">
                </form>
            </div>

            <div class="manage-spacer"></div>

            <div class="manage-default-people">
                <a class="manage-default-people-link" href="/manager/default">
                    <button class="manage-default-people-button">Ladda in klassen</button>
                </a>
            </div>

        </div>

    </div>
</div>